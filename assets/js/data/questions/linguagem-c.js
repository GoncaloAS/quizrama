/**
 * Banco de perguntas — Linguagem C
 * Total: 44 perguntas.
 *
 * Esquema de cada entrada:
 *   [topic, id, questionHtml, [4 options], correctIdx, explanationHtml, hintHtml?]
 */
/* eslint-disable */
const Q_LINGUAGEM_C = [
  [
    "Linguagem C",
    "1",
    "Qual a sequência correcta de fases na compilação de um programa em C?",
    [
      "Compilador → Linker → Pré-processador → Loader",
      "Pré-processador → Linker → Compilador → Loader",
      "Compilador → Pré-processador → Linker → Loader",
      "Pré-processador → Compilador → Linker → Loader",
    ],
    3,
    "<b>(D) ✓ correcta:</b> a ordem é Pré-processador → Compilador → Linker → Loader.\n\n• <b>Pré-processador</b>: resolve <code>#include</code>, <code>#define</code>, comentários e directivas. Output: ficheiro <code>.i</code>.\n• <b>Compilador</b>: traduz C → assembly → código-objecto (<code>.o</code>).\n• <b>Linker</b>: junta vários <code>.o</code> + bibliotecas, resolve referências entre eles → executável.\n• <b>Loader</b>: carrega o executável em memória RAM e dá-lhe controlo (acontece em runtime).\n\n<b>(C), (B), (A) ✗ erradas:</b> qualquer outra ordem é impossível — o compilador não pode trabalhar antes do pré-processador (ainda não resolveu macros), e o linker não pode trabalhar antes do compilador (não há <code>.o</code> para juntar).",
    "Mnemónica: <b>P</b>ré → <b>C</b>ompilar → <b>L</b>inkar → <b>L</b>oad.<br><a href=\"sistemas_operacoes_estudo__2_.html#1-compilação-em-c\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §1 Compilação em C ↗</a>",
  ],
  [
    "Linguagem C",
    "2",
    "Qual a flag do <code>gcc</code> que permite incluir símbolos de debug no executável?",
    [
      "-Wall",
      "-c",
      "-g",
      "-O2",
    ],
    2,
    "<b>(C) -g ✓ correcta:</b> adiciona ao binário a tabela de símbolos (nomes de variáveis, números de linha, mapeamento código→fonte) que ferramentas como o <code>gdb</code> precisam para fazer breakpoints, step, inspecção de variáveis.\n\n<b>(D) -O2 ✗</b>: nível de optimização do código (rapidez/tamanho). Pode até <i>complicar</i> o debug porque o compilador reordena instruções.\n<b>(A) -Wall ✗</b>: activa warnings durante a compilação. Não tem nada a ver com debug em runtime.\n<b>(B) -c ✗</b>: para o processo após o compilador (não corre o linker). Útil para gerar <code>.o</code>, não para debug.",
    "<b>g</b> = de<b>g</b>ug. Em produção remove-se (binário fica mais pequeno e protege contra reverse-engineering).<br><a href=\"sistemas_operacoes_estudo__2_.html#1-compilação-em-c\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §1 Compilação em C ↗</a>",
  ],
  [
    "Linguagem C",
    "3",
    "Qual a flag do <code>gcc</code> que activa todos os warnings comuns?",
    [
      "-Wextra",
      "-w",
      "-Wall",
      "-Werror",
    ],
    2,
    "<b>(C) -Wall ✓ correcta:</b> \"<i>Warnings: all (the common ones)</i>\" — activa o conjunto standard de avisos: variáveis não usadas, comparações suspeitas, return em falta, etc.\n\n<b>(B) -w ✗ pegadinha clássica:</b> minúsculo <i>desliga</i> TODOS os warnings (oposto do que se quer).\n<b>(D) -Werror ✗</b>: trata os warnings como <i>erros</i>, parando a compilação. Não os \"activa\" — apenas escala a severidade dos que já estão activos.\n<b>(A) -Wextra ✗</b>: activa avisos <i>adicionais</i> além dos do <code>-Wall</code> (uso típico: <code>-Wall -Wextra</code> juntos). Por si só, não inclui o conjunto base.",
    "<code>-w</code> minúsculo silencia, <code>-Wall</code> activa. Não confundir.<br><a href=\"sistemas_operacoes_estudo__2_.html#1-compilação-em-c\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §1 Compilação em C ↗</a>",
  ],
  [
    "Linguagem C",
    "4",
    "O que faz a flag <code>-c</code> do <code>gcc</code>?",
    [
      "Compila e linka, gerando executável",
      "Activa modo de debug",
      "Compila apenas, parando antes da linkagem",
      "Limpa ficheiros temporários",
    ],
    2,
    "<b>(C) ✓ correcta:</b> <code>-c</code> = \"<i>compile only</i>\". Gera o ficheiro-objecto <code>.o</code> mas não invoca o linker. Útil para projectos grandes — recompilas só os <code>.c</code> que mudaram, depois fazes link no fim.\n\n<b>(A) ✗</b>: esse é o comportamento <i>por defeito</i>, sem <code>-c</code>.\n<b>(D) ✗</b>: limpeza não é função do compilador (faz-se com <code>make clean</code> ou <code>rm</code>).\n<b>(B) ✗</b>: debug é a flag <code>-g</code>, não <code>-c</code>.",
    "Padrão típico: <code>gcc -c a.c</code>; <code>gcc -c b.c</code>; <code>gcc a.o b.o -o prog</code>.<br><a href=\"sistemas_operacoes_estudo__2_.html#1-compilação-em-c\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §1 Compilação em C ↗</a>",
  ],
  [
    "Linguagem C",
    "5",
    "O que retorna o operador <code>sizeof()</code> quando aplicado a uma variável?",
    [
      "O endereço da variável",
      "O número de bits que ocupa",
      "O número de bytes que ocupa em memória",
      "O valor da variável",
    ],
    2,
    "<b>(C) ✓ correcta:</b> <code>sizeof</code> devolve o tamanho em <i>bytes</i> que o tipo/variável ocupa. Avaliado em <i>tempo de compilação</i> (excepto para VLAs em C99). Ex: <code>sizeof(int)</code> tipicamente dá 4 (em arquitecturas 32/64-bit).\n\n<b>(B) ✗</b>: bits não — para obter bits multiplica-se por 8 (<code>sizeof(x) * 8</code>) usando a constante <code>CHAR_BIT</code> em <code>&lt;limits.h&gt;</code>.\n<b>(A) ✗</b>: o endereço é dado pelo operador <code>&</code>, não <code>sizeof</code>.\n<b>(D) ✗</b>: o valor obtém-se simplesmente usando a variável; <code>sizeof</code> ignora o valor (nem sequer avalia a expressão).",
    "Pegadinha: <code>sizeof(arr)</code> num array dá tamanho do array, mas se passares o array a uma função, esta vê-o como apontador → <code>sizeof</code> dá o tamanho do apontador (8 em 64-bit), não do array.<br><a href=\"sistemas_operacoes_estudo__2_.html#2-tipos-de-dados\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §2 Tipos de Dados ↗</a>",
  ],
  [
    "Linguagem C",
    "6",
    "Qual destes tipos NÃO é um tipo inteiro padrão em C?",
    [
      "unsigned int",
      "short",
      "long",
      "real",
    ],
    3,
    "<b>(D) real ✓ correcta:</b> a palavra \"real\" não existe na linguagem C. Para vírgula flutuante usam-se <code>float</code> (32 bits), <code>double</code> (64 bits) ou <code>long double</code> (80–128 bits, depende da arquitectura).\n\n<b>(B) short ✓ inteiro</b>: tipicamente 16 bits (no mínimo). Range: −32 768 a 32 767.\n<b>(C) long ✓ inteiro</b>: tipicamente 32 ou 64 bits (depende da plataforma e modelo de dados — ILP32 vs LP64).\n<b>(A) unsigned int ✓ inteiro</b>: variante de <code>int</code> sem sinal. Range: 0 a 2³²−1 (4 294 967 295) em sistemas com <code>int</code> de 32 bits.",
    "Inteiros em C: <code>char</code>, <code>short</code>, <code>int</code>, <code>long</code>, <code>long long</code>, todos com variantes <code>signed</code>/<code>unsigned</code>.<br><a href=\"sistemas_operacoes_estudo__2_.html#2-tipos-de-dados\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §2 Tipos de Dados ↗</a>",
  ],
  [
    "Linguagem C",
    "7",
    "Qual a diferença entre <code>float</code> e <code>double</code>?",
    [
      "double só pode armazenar valores inteiros",
      "São equivalentes em precisão e tamanho",
      "float tem maior precisão que double",
      "double tem maior precisão (64 bits) que float (32 bits)",
    ],
    3,
    "<b>(D) ✓ correcta:</b> \"<i>double precision floating point</i>\". Em IEEE 754:\n• <b>float</b>: 32 bits (1 sinal + 8 expoente + 23 mantissa) → ~7 dígitos decimais de precisão.\n• <b>double</b>: 64 bits (1 + 11 + 52) → ~15 dígitos decimais.\nO default em C para um literal como <code>3.14</code> é <code>double</code> — para forçar <code>float</code> escreve-se <code>3.14f</code>.\n\n<b>(B) ✗</b>: tamanhos e precisões diferentes.\n<b>(C) ✗</b>: invertido — <code>double</code> tem o dobro dos bits do <code>float</code>.\n<b>(A) ✗</b>: <code>double</code> é vírgula flutuante (não-inteiro). Para inteiros usa-se <code>int</code>/<code>long</code>.",
    "Em sistemas embedded com pouca RAM/CPU, ainda se usa <code>float</code>. Em CPUs modernas, <code>double</code> é igualmente rápido (FPU é 64-bit nativo).<br><a href=\"sistemas_operacoes_estudo__2_.html#2-tipos-de-dados\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §2 Tipos de Dados ↗</a>",
  ],
  [
    "Linguagem C",
    "8",
    "Para que serve o tipo <code>void</code> em C?",
    [
      "Indica um tipo numérico de precisão dupla",
      "Indica um inteiro de 0 bits",
      "É equivalente a <code>int</code>",
      "Indica ausência de tipo",
    ],
    3,
    "<b>(D) ✓ correcta:</b> <code>void</code> = \"nada/sem tipo\". Três usos:\n1. <b>Tipo de retorno</b>: <code>void func(...)</code> — função não devolve nada.\n2. <b>Lista de parâmetros vazia</b>: <code>int main(void)</code> — função não recebe argumentos.\n3. <b>Apontador genérico</b>: <code>void *p</code> — pode apontar para qualquer tipo (mas requer cast antes de dereferenciar).\n\n<b>(A) ✗</b>: precisão dupla é <code>double</code>.\n<b>(B) ✗</b>: <code>void</code> não tem tamanho (<code>sizeof(void)</code> é mesmo erro de compilação na maioria dos compiladores).\n<b>(C) ✗</b>: <code>int</code> tem valor (0, 5, −10…); <code>void</code> não.",
    "<code>malloc</code> devolve <code>void *</code> precisamente porque não sabe que tipo vais alocar. Tu fazes o cast: <code>int *p = malloc(sizeof(int)*10);</code> (em C o cast é opcional, em C++ é obrigatório).<br><a href=\"sistemas_operacoes_estudo__2_.html#2-tipos-de-dados\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §2 Tipos de Dados ↗</a>",
  ],
  [
    "Linguagem C",
    "9",
    "O que produz <code>5 % 2</code> em C?",
    [
      "2",
      "2.5",
      "1",
      "0",
    ],
    2,
    "<b>(C) 1 ✓ correcta:</b> <code>%</code> é o operador <i>módulo</i> (resto da divisão inteira). 5 ÷ 2 = 2 com resto 1.\n\n<b>(B) 2.5 ✗</b>: seria o resultado de <code>5.0 / 2.0</code> (divisão real). <code>%</code> não é divisão — é apenas o resto.\n<b>(A) 2 ✗</b>: é o quociente da divisão inteira (<code>5 / 2</code> em C dá 2). Pegadinha clássica: confundir quociente com resto.\n<b>(D) 0 ✗</b>: só seria 0 se a divisão fosse exacta (ex: <code>4 % 2 = 0</code>).",
    "<code>%</code> só funciona com <b>inteiros</b>. Para reais usa <code>fmod()</code> de <code>&lt;math.h&gt;</code>. Cuidado com negativos: em C o sinal do resto segue o dividendo (<code>-5 % 2 = -1</code>).<br><a href=\"sistemas_operacoes_estudo__2_.html#3-operadores\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §3 Operadores ↗</a>",
  ],
  [
    "Linguagem C",
    "10",
    "Qual é o operador lógico AND em C?",
    [
      "and",
      "&&",
      "&",
      "AND",
    ],
    1,
    "<b>(B) && ✓ correcta:</b> AND lógico, com avaliação <i>curto-circuito</i> — se o operando esquerdo for falso, o direito nem é avaliado. Ex: <code>if (p != NULL && p->x &gt; 0)</code> é seguro porque o <code>p->x</code> só corre se <code>p</code> não for NULL.\n\n<b>(C) & ✗</b>: AND <i>bitwise</i> (a nível de bits). <code>5 & 3</code> = <code>0101 AND 0011</code> = <code>0001</code> = 1. NÃO faz curto-circuito.\n<b>(D) AND ✗</b>: não existe em C (existe em SQL, Pascal, etc.).\n<b>(A) and ✗</b>: também não existe em C standard. Existe como alternativa em C++ (via <code>&lt;iso646.h&gt;</code>) mas é raríssimo.",
    "Resumo: lógicos = <code>&&</code> <code>||</code> <code>!</code>. Bitwise = <code>&</code> <code>|</code> <code>^</code> <code>~</code>. Trocar os dois é bug clássico.<br><a href=\"sistemas_operacoes_estudo__2_.html#3-operadores\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §3 Operadores ↗</a>",
  ],
  [
    "Linguagem C",
    "11",
    "O que faz o operador bitwise <code>&lt;&lt;</code> (shift left)?",
    [
      "Compara dois valores",
      "Desloca os bits para a esquerda",
      "Faz uma divisão inteira",
      "Atribui um valor",
    ],
    1,
    "<b>(B) ✓ correcta:</b> <code>x &lt;&lt; n</code> desloca os bits de <code>x</code> n posições para a esquerda, preenchendo com zeros à direita. Equivale matematicamente a <code>x × 2ⁿ</code>. Ex: <code>1 &lt;&lt; 3</code> = <code>00001000</code> = 8.\n\n<b>(A) ✗</b>: comparação é <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&gt;</code>.\n<b>(D) ✗</b>: atribuição é <code>=</code>. (Existe <code>&lt;&lt;=</code> que é shift+atribuição.)\n<b>(C) ✗</b>: divisão é <code>/</code>. <i>Shift right</i> (<code>&gt;&gt;</code>) é que equivale a divisão por 2ⁿ — confusão fácil.",
    "Útil para flags binárias e máscaras: <code>1 &lt;&lt; 5</code> dá uma máscara só com o bit 5 ligado. Cuidado: shift por &gt;= largura do tipo é <i>undefined behavior</i>.<br><a href=\"sistemas_operacoes_estudo__2_.html#3-operadores\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §3 Operadores ↗</a>",
  ],
  [
    "Linguagem C",
    "12",
    "Qual é a diferença entre <code>==</code> e <code>=</code> em C?",
    [
      "<code>==</code> só funciona com strings",
      "<code>==</code> é comparação de igualdade",
      "<code>=</code> é comparação; <code>==</code> é atribuição",
      "São equivalentes",
    ],
    1,
    "<b>(B) ✓ correcta:</b> <code>=</code> atribui um valor a uma variável (<code>x = 5</code>). <code>==</code> compara dois valores e devolve 0 ou 1 (<code>x == 5</code>).\n\n<b>(D) ✗</b>: completamente diferentes — confundi-los é o bug clássico de C.\n<b>(C) ✗</b>: invertido. Pegadinha: <code>if (x = 5)</code> compila SEM erro mas <i>atribui</i> 5 a x e a condição é 5 (truthy) → ramo sempre executado. Solução: alguns programadores escrevem <code>if (5 == x)</code> (<i>Yoda condition</i>) para que <code>if (5 = x)</code> dê erro de compilação.\n<b>(A) ✗</b>: <code>==</code> em strings <i>compara apontadores</i>, não conteúdo. Para comparar conteúdo usa-se <code>strcmp()</code>.",
    "Mnemónica: 1 igual = atribui, 2 iguais = compara. Compiladores modernos avisam (<code>-Wall</code>) se vires <code>if (x = 5)</code>.<br><a href=\"sistemas_operacoes_estudo__2_.html#3-operadores\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §3 Operadores ↗</a>",
  ],
  [
    "Linguagem C",
    "13",
    "Numa instrução <code>switch</code>, o que acontece se omitires <code>break</code> no fim de um caso?",
    [
      "Erro de compilação",
      "É feito automaticamente um <code>break</code> implícito",
      "Ocorre <i>fall-through</i>",
      "O programa termina",
    ],
    2,
    "<b>(C) ✓ correcta:</b> sem <code>break</code>, a execução \"cai\" (<i>fall through</i>) para o caso seguinte e continua até encontrar um <code>break</code> ou o fim do <code>switch</code>. Às vezes é desejado (agrupar casos), mas geralmente é fonte de bugs subtis.\n\n<b>(A) ✗</b>: o compilador não dá erro — fall-through é comportamento legal. (Compiladores modernos podem dar <i>warning</i> com <code>-Wall</code> a sugerir o atributo <code>[[fallthrough]]</code> em C23.)\n<b>(D) ✗</b>: não termina — só executa o próximo case.\n<b>(B) ✗</b>: NÃO há <code>break</code> implícito (ao contrário, por exemplo, de Pascal).",
    "Convenção: pôr o <code>default</code> no fim. Para fall-through deliberado, comentar (<code>// fallthrough</code>) para deixar a intenção clara.<br><a href=\"sistemas_operacoes_estudo__2_.html#4-estruturas-de-decisão-e-ciclos\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §4 Estruturas de Decisão e Ciclos ↗</a>",
  ],
  [
    "Linguagem C",
    "14",
    "Qual a diferença fundamental entre <code>while</code> e <code>do/while</code>?",
    [
      "while garante pelo menos uma execução do corpo",
      "do/while executa o corpo pelo menos uma vez (testa no fim)",
      "do/while é internamente mais rápido que while",
      "while testa no fim, do/while testa no início",
    ],
    1,
    "<b>(B) ✓ correcta:</b>\n• <code>while (cond) { ... }</code>: testa <i>antes</i>. Se <code>cond</code> for falsa logo no início, o corpo nunca executa (zero iterações).\n• <code>do { ... } while (cond);</code>: testa <i>depois</i>. O corpo executa pelo menos uma vez (uma ou mais iterações).\n\n<b>(D) ✗</b>: têm semânticas diferentes — escolher mal cria bugs (ex: ler input do utilizador exige pelo menos uma leitura → usa <code>do/while</code>).\n<b>(A) ✗</b>: invertido — é o <code>do/while</code> que garante ≥1 execução.\n<b>(C) ✗</b>: performance é praticamente igual; o compilador gera código essencialmente equivalente.",
    "Padrão típico do <code>do/while</code>: validar input do utilizador (<code>do { lê } while (input inválido)</code>).<br><a href=\"sistemas_operacoes_estudo__2_.html#4-estruturas-de-decisão-e-ciclos\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §4 Estruturas de Decisão e Ciclos ↗</a>",
  ],
  [
    "Linguagem C",
    "15",
    "Numa estrutura <code>for (int i=0; i&lt;10; i++)</code>, o que representa cada parte?",
    [
      "condição; inicialização; incremento",
      "incremento; condição; inicialização",
      "inicialização; condição; incremento",
      "inicialização; incremento; condição",
    ],
    2,
    "<b>(C) ✓ correcta:</b> ordem é <code>for (init; cond; step)</code>:\n1. <b>init</b>: executa UMA vez antes do ciclo começar.\n2. <b>cond</b>: avaliada antes de cada iteração; se falsa, sai do ciclo.\n3. <b>step</b>: executa no FIM de cada iteração, antes de re-avaliar a condição.\n\nEquivalente a:\n<code>init;<br>while (cond) {<br>&nbsp;&nbsp;corpo;<br>&nbsp;&nbsp;step;<br>}</code>\n\n<b>(D), (A), (B) ✗</b>: as três expressões existem mas qualquer outra ordem está errada — a sintaxe do <code>for</code> em C é fixa.",
    "As três partes são opcionais: <code>for (;;)</code> é loop infinito legal. Variáveis declaradas em <code>init</code> (C99+) só existem dentro do <code>for</code>.<br><a href=\"sistemas_operacoes_estudo__2_.html#4-estruturas-de-decisão-e-ciclos\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §4 Estruturas de Decisão e Ciclos ↗</a>",
  ],
  [
    "Linguagem C",
    "16",
    "O que armazena uma variável do tipo \"apontador para int\" (<code>int *p</code>)?",
    [
      "O valor de um inteiro",
      "O tamanho de um inteiro",
      "O nome de uma variável inteira",
      "O endereço de memória onde está armazenado um inteiro",
    ],
    3,
    "<b>(D) ✓ correcta:</b> apontadores armazenam <i>endereços</i>, não valores. <code>int *p</code> declara que <code>p</code> é um apontador, e o tipo do que ele aponta é <code>int</code>. <code>p</code> em si é um endereço (8 bytes em arquitecturas de 64-bit).\n\n<b>(A) ✗</b>: o valor inteiro está noutro sítio (no endereço apontado). Para chegar a ele faz-se <code>*p</code>.\n<b>(B) ✗</b>: o tamanho obtém-se com <code>sizeof(int)</code> ou <code>sizeof(*p)</code>, não está armazenado em <code>p</code>.\n<b>(C) ✗</b>: nomes de variáveis só existem em tempo de compilação — em runtime, só existem endereços.",
    "Truque para ler declarações: <code>int *p</code> lê-se \"<code>*p</code> é <code>int</code>\" → logo <code>p</code> é \"apontador para int\".<br><a href=\"sistemas_operacoes_estudo__2_.html#5-apontadores-pointers\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §5 Apontadores (Pointers) ↗</a>",
  ],
  [
    "Linguagem C",
    "17",
    "Em C, qual é o efeito do operador <code>&amp;</code> quando aplicado a uma variável?",
    [
      "Faz uma cópia da variável",
      "Retorna o endereço de memória da variável",
      "Dereferencia a variável",
      "Faz AND lógico",
    ],
    1,
    "<b>(B) ✓ correcta:</b> <code>&amp;var</code> é o operador <i>address-of</i> — devolve o endereço de memória onde <code>var</code> está. Tipicamente usado para inicializar apontadores: <code>int *p = &amp;x;</code> ou para passar argumentos por referência: <code>scanf(\"%d\", &amp;n);</code>.\n\n<b>(D) ✗</b>: AND lógico é <code>&amp;&amp;</code> (dois ampersand). Um único <code>&amp;</code> em contexto binário (<code>x &amp; y</code>) é <i>bitwise AND</i>; em contexto unário (<code>&amp;x</code>) é <i>address-of</i>.\n<b>(C) ✗</b>: dereferenciar é <code>*</code> (asterisco), não <code>&amp;</code>.\n<b>(A) ✗</b>: cópia faz-se com atribuição (<code>y = x</code>) ou <code>memcpy()</code>.",
    "Pensa: <code>&amp;</code> e <code>*</code> são operações <i>opostas</i>. <code>*(&amp;x)</code> dá <code>x</code> (vai ao endereço, lê o valor).<br><a href=\"sistemas_operacoes_estudo__2_.html#5-apontadores-pointers\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §5 Apontadores (Pointers) ↗</a>",
  ],
  [
    "Linguagem C",
    "18",
    "Em C, qual é o efeito do operador <code>*</code> quando aplicado a um apontador?",
    [
      "Dereferencia o apontador",
      "Liberta o espaço apontado",
      "Multiplica o apontador por 2",
      "Retorna o endereço da variável apontada",
    ],
    0,
    "<b>(A) ✓ correcta:</b> <code>*p</code> dá o <i>valor armazenado no endereço apontado por p</i>. Se <code>p</code> aponta para um <code>int</code> com valor 42, então <code>*p == 42</code>. Pode estar à esquerda da atribuição: <code>*p = 99</code> escreve 99 nesse endereço.\n\n<b>(C) ✗</b>: multiplicação é binária (<code>x * 2</code>); <code>*</code> unário (na frente de uma variável) é dereferência. O contexto distingue.\n<b>(D) ✗</b>: o endereço já está em <code>p</code> — <code>*</code> faz o oposto: vai ao endereço, lê o valor.\n<b>(B) ✗</b>: libertar memória é <code>free(p)</code>, não <code>*p</code>.",
    "<code>*</code> aparece em 2 contextos diferentes: na <i>declaração</i> (<code>int *p</code>) marca o tipo; no <i>uso</i> (<code>*p = 5</code>) faz dereferência. Não confundir.<br><a href=\"sistemas_operacoes_estudo__2_.html#5-apontadores-pointers\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §5 Apontadores (Pointers) ↗</a>",
  ],
  [
    "Linguagem C",
    "19",
    "O que é um apontador <code>NULL</code>?",
    [
      "Um apontador que não foi inicializado",
      "Um apontador que aponta deliberadamente para \"nada\"",
      "Um apontador apenas para inteiros",
      "Um apontador que aponta para o início da memória",
    ],
    1,
    "<b>(B) ✓ correcta:</b> <code>NULL</code> é uma macro definida em <code>&lt;stddef.h&gt;</code> (e indirectamente em <code>&lt;stdio.h&gt;</code>, <code>&lt;stdlib.h&gt;</code>...) tipicamente como <code>((void*)0)</code>. Convenção universal: apontador inválido / não atribuído / \"ponteiro vazio\".\n\n<b>(A) ✗ pegadinha importante:</b> NULL e \"não inicializado\" são coisas diferentes. Um apontador não inicializado tem <i>lixo</i> aleatório (pode apontar para qualquer sítio); um apontador NULL aponta deliberadamente para \"nada\". Boa prática: inicializar SEMPRE como NULL: <code>int *p = NULL;</code>.\n<b>(D) ✗</b>: o endereço 0 está reservado pelo SO — qualquer acesso lança <i>segmentation fault</i>. NULL ser tipicamente 0 é uma convenção, não significa que aponta para \"início da memória\".\n<b>(C) ✗</b>: NULL é genérico — funciona para apontadores de qualquer tipo.",
    "Sempre verificar antes de dereferenciar: <code>if (p != NULL) *p = ...;</code>. <code>malloc</code> devolve NULL se falhar — tem de se verificar.<br><a href=\"sistemas_operacoes_estudo__2_.html#5-apontadores-pointers\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §5 Apontadores (Pointers) ↗</a>",
  ],
  [
    "Linguagem C",
    "20",
    "Em C, qual é o índice do <i>primeiro</i> elemento de um array <code>int arr[10]</code>?",
    [
      "1",
      "0",
      "depende do compilador",
      "-1",
    ],
    1,
    "<b>(B) 0 ✓ correcta:</b> arrays em C são indexados a partir de <i>zero</i>. <code>arr[0]</code> é o primeiro elemento, <code>arr[9]</code> é o último (<code>arr[10]</code> é <i>fora dos limites</i> — undefined behavior).\n\nMotivo histórico/técnico: <code>arr[i]</code> é apenas açúcar sintático para <code>*(arr + i)</code> — o \"deslocamento\" do primeiro elemento é zero.\n\n<b>(A) ✗</b>: começar em 1 é Pascal/Lua/MATLAB, não C.\n<b>(D) ✗</b>: índices negativos não são válidos (excepto via apontador, em truques específicos).\n<b>(C) ✗</b>: standard fixo do C — sempre 0-indexed.",
    "Bug clássico: <code>for (i=0; i&lt;=10; i++)</code> num array de tamanho 10 acessa <code>arr[10]</code> — fora dos limites.<br><a href=\"sistemas_operacoes_estudo__2_.html#6-arrays\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §6 Arrays ↗</a>",
  ],
  [
    "Linguagem C",
    "21",
    "Em C, qual é o índice do primeiro elemento de um array?",
    [
      "0",
      "-1",
      "1",
      "depende do compilador",
    ],
    0,
    "<b>(A) 0 ✓ correcta:</b> arrays em C são <i>0-indexed</i>. Para um array de tamanho N, índices vão de 0 até N−1. Ex: <code>int a[5]</code> tem <code>a[0], a[1], a[2], a[3], a[4]</code>.\n\n<b>(C) 1 ✗</b>: linguagens como Lua, Fortran e MATLAB usam 1-based, mas C/C++/Java/Python/JavaScript não.\n<b>(B) -1 ✗</b>: aceder a <code>a[-1]</code> em C é <i>undefined behavior</i> (lê fora do array).\n<b>(D) ✗</b>: o standard C define-o como 0 — não há liberdade para o compilador alterar isto.",
    "Pegadinha clássica: ciclo <code>for (i=0; i&lt;=N; i++)</code> com <code>&lt;=</code> em vez de <code>&lt;</code> escreve fora do array (<i>off-by-one</i> bug). Lê SEMPRE como \"<code>i menor que N</code>\".<br><a href=\"sistemas_operacoes_estudo__2_.html#6-arrays\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §6 Arrays ↗</a>",
  ],
  [
    "Linguagem C",
    "22",
    "Como é representada uma string em C?",
    [
      "Como uma classe <code>std::string</code>",
      "Como um apontador para <code>int</code>",
      "Como um array de caracteres terminado pelo byte zero",
      "Como um tipo nativo <code>string</code>",
    ],
    2,
    "<b>(C) ✓ correcta:</b> em C uma string é simplesmente um <code>char[]</code> com um byte zero (zero binário) no fim. O byte zero marca onde a string acaba — funções como <code>printf</code>, <code>strlen</code>, <code>strcpy</code> param ali.\n\nEx: <code>\"abc\"</code> ocupa 4 bytes na memória: <code>'a' 'b' 'c' 0</code>.\n\n<b>(D) ✗</b>: C não tem tipo <code>string</code> nativo (Java, C#, Python têm — C não).\n<b>(A) ✗</b>: <code>std::string</code> é C++, não C. Confusão comum.\n<b>(B) ✗</b>: chars não são ints. Embora um <code>char</code> seja 1 byte (caso especial de inteiro), a string é <i>array de chars</i>, não de ints.",
    "Esquecer o byte zero é a fonte n.º 1 de bugs de strings em C. <code>strcpy</code> escreve até encontrar o byte zero — se não houver, escreve infinitamente até crashar.<br><a href=\"sistemas_operacoes_estudo__2_.html#7-strings\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §7 Strings ↗</a>",
  ],
  [
    "Linguagem C",
    "23",
    "Qual a função correcta de <code>&lt;string.h&gt;</code> para copiar uma string?",
    [
      "strcmp",
      "strcpy",
      "strcat",
      "strlen",
    ],
    1,
    "<b>(B) strcpy ✓ correcta:</b> <code>strcpy(dest, src)</code> copia caracteres de <code>src</code> para <code>dest</code>, incluindo o byte zero terminador final. <i>Cuidado</i>: não verifica se <code>dest</code> tem espaço suficiente — buffer overflow é fácil. Versão segura: <code>strncpy(dest, src, n)</code> (mas tem armadilhas próprias).\n\n<b>(C) strcat ✗</b>: <i>concatena</i> (anexa <code>src</code> ao fim de <code>dest</code>). Útil mas não é \"copiar\".\n<b>(A) strcmp ✗</b>: <i>compara</i> duas strings. Devolve 0 se iguais, &lt;0 ou &gt;0 conforme ordem lexicográfica.\n<b>(D) strlen ✗</b>: devolve o <i>comprimento</i> (número de chars antes do byte zero).",
    "Mnemónica: <b>cpy</b>=copy, <b>cat</b>=concatenate, <b>cmp</b>=compare, <b>len</b>=length. Os nomes das funções são consistentes.<br><a href=\"sistemas_operacoes_estudo__2_.html#7-strings\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §7 Strings ↗</a>",
  ],
  [
    "Linguagem C",
    "24",
    "O que retorna <code>strlen(\"hello\")</code> em C?",
    [
      "7",
      "4",
      "6 (incluindo o terminador)",
      "5",
    ],
    3,
    "<b>(D) 5 ✓ correcta:</b> <code>strlen</code> conta os caracteres da string até (mas não incluindo) o byte zero terminador. \"hello\" tem 5 caracteres, logo <code>strlen(\"hello\") == 5</code>.\n\n<b>(C) 6 ✗ pegadinha:</b> 6 é o <i>tamanho em memória</i> (5 chars + terminador), mas <code>strlen</code> não conta o terminador. Para alocar memória usas 6, para iterar chars usas 5.\n<b>(B) 4 ✗</b>: 5 caracteres em \"hello\", não 4.\n<b>(A) 7 ✗</b>: nenhuma interpretação válida dá 7.",
    "Pegadinha: <code>sizeof(\"hello\")</code> dá 6 (inclui o terminador), mas <code>strlen(\"hello\")</code> dá 5. Se vires diferença de 1, lembra-te qual é qual.<br><a href=\"sistemas_operacoes_estudo__2_.html#7-strings\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §7 Strings ↗</a>",
  ],
  [
    "Linguagem C",
    "25",
    "Em C, por defeito a passagem de argumentos a funções é:",
    [
      "Por referência",
      "Por valor",
      "Por endereço sempre",
      "Por nome",
    ],
    1,
    "<b>(B) ✓ correcta:</b> C usa <i>sempre</i> call-by-value. Dentro da função recebes uma <i>cópia</i> do argumento — modificá-la não afecta o original do chamador.\n\nEx: <code>void f(int x) { x = 100; }</code><br><code>int n = 5; f(n); // n continua 5</code>\n\n<b>(A) ✗</b>: C++ tem referências (<code>int &amp;x</code>); C não. Em C simula-se com apontadores.\n<b>(D) ✗</b>: \"by name\" é semântica esotérica de Algol — não existe em linguagens modernas.\n<b>(C) ✗</b>: arrays são tratados como apontadores (<i>decay</i>), o que dá ilusão de \"by reference\", mas tecnicamente passa-se uma cópia do <i>endereço</i> — ainda é by-value (do apontador).",
    "Para alterar uma variável dentro de uma função, passa o seu endereço: <code>void inc(int *p) { (*p)++; }</code> chamado como <code>inc(&amp;n);</code>.<br><a href=\"sistemas_operacoes_estudo__2_.html#8-funções--call-by-value-vs-call-by-reference\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §8 Funções — Call by Value vs Call by Reference ↗</a>",
  ],
  [
    "Linguagem C",
    "26",
    "Como podes simular passagem por referência em C?",
    [
      "Marcando a função como inline",
      "Marcando o parâmetro com & na declaração",
      "Passando o endereço da variável (apontador)",
      "Não é possível simular em C standard",
    ],
    2,
    "<b>(C) ✓ correcta:</b> padrão clássico em C. O chamador passa o endereço com <code>&amp;</code>, a função recebe um apontador com <code>*</code> e dereferencia para alterar o original.\n\nEx: <code>void swap(int *a, int *b) {<br>&nbsp;&nbsp;int t = *a;<br>&nbsp;&nbsp;*a = *b;<br>&nbsp;&nbsp;*b = t;<br>}</code>\n\nChamado como <code>swap(&amp;x, &amp;y);</code>\n\n<b>(D) ✗</b>: é possível — basta usar apontadores.\n<b>(B) ✗</b>: <code>&amp;</code> em declarações de parâmetros é sintaxe de C++, não C. Em C dá erro de compilação.\n<b>(A) ✗</b>: <code>inline</code> é uma sugestão de optimização para o compilador (substituir a chamada pelo corpo). Nada tem a ver com semântica de passagem.",
    "Funções da libc usam isto: <code>scanf(\"%d\", &amp;n)</code> passa o endereço para <code>scanf</code> poder escrever em <code>n</code>.<br><a href=\"sistemas_operacoes_estudo__2_.html#8-funções--call-by-value-vs-call-by-reference\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §8 Funções — Call by Value vs Call by Reference ↗</a>",
  ],
  [
    "Linguagem C",
    "27",
    "Qual a função de <code>&lt;stdlib.h&gt;</code> usada para alocar memória dinamicamente em runtime?",
    [
      "create()",
      "alloc()",
      "malloc()",
      "new",
    ],
    2,
    "<b>(C) malloc() ✓ correcta:</b> \"<i>memory allocate</i>\". <code>malloc(n)</code> reserva <code>n</code> bytes contíguos no <i>heap</i> e devolve um <code>void *</code> para o início do bloco. Devolve <code>NULL</code> se não houver memória disponível.\n\n<b>(B) alloc() ✗</b>: não existe na libc standard.\n<b>(D) new ✗</b>: operador de C++, não C. Em C++ usa-se <code>new</code>/<code>delete</code>; em C usa-se <code>malloc</code>/<code>free</code>.\n<b>(A) create() ✗</b>: também não existe (existem funções com este nome em outros contextos como <code>pthread_create</code>, mas não para memória).",
    "Família completa em <code>&lt;stdlib.h&gt;</code>: <code>malloc</code> (alocar), <code>calloc</code> (alocar+zerar), <code>realloc</code> (redimensionar), <code>free</code> (libertar). Toda <code>malloc</code> precisa de uma <code>free</code> correspondente.<br><a href=\"sistemas_operacoes_estudo__2_.html#9-alocação-dinâmica-de-memória\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §9 Alocação Dinâmica de Memória ↗</a>",
  ],
  [
    "Linguagem C",
    "28",
    "Qual é a diferença entre <code>malloc(n)</code> e <code>calloc(n, size)</code>?",
    [
      "Funcionalmente idênticos, só nomes diferentes",
      "calloc é mais rápido por usar mmap",
      "malloc aloca na stack; calloc na heap",
      "malloc não inicializa; calloc inicializa a zero",
    ],
    3,
    "<b>(D) ✓ correcta:</b>\n• <code>malloc(n)</code> → reserva n bytes; conteúdo é <i>lixo</i> (valores aleatórios deixados pela memória anterior).\n• <code>calloc(n, size)</code> → reserva n × size bytes <i>e zera tudo</i>. Útil para arrays inteiros e structs onde queres começar com 0/NULL.\n\n<b>(A) ✗</b>: comportamento (zerar ou não) é diferente.\n<b>(B) ✗</b>: <i>invertido</i> — <code>calloc</code> é mais lento porque tem de escrever zeros. Em sistemas modernos com <i>lazy zero pages</i>, a diferença pode ser pequena, mas <code>calloc</code> nunca é mais rápido.\n<b>(C) ✗</b>: ambos alocam no <i>heap</i>. Stack é gerida pelo compilador (variáveis locais).",
    "Use <code>calloc</code> quando quiseres zeros (mais legível que <code>malloc + memset</code>). Use <code>malloc</code> quando vais sobrescrever tudo (não pagas pelo zero).<br><a href=\"sistemas_operacoes_estudo__2_.html#9-alocação-dinâmica-de-memória\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §9 Alocação Dinâmica de Memória ↗</a>",
  ],
  [
    "Linguagem C",
    "29",
    "O que faz <code>free(p)</code> em C?",
    [
      "Apaga a variável p",
      "Põe p automaticamente a NULL",
      "Liberta a memória dinâmica apontada por p",
      "Imprime o conteúdo de p",
    ],
    2,
    "<b>(C) ✓ correcta:</b> <code>free(p)</code> devolve ao gestor de memória do sistema o bloco que foi previamente alocado por <code>malloc</code>/<code>calloc</code>/<code>realloc</code> em <code>p</code>. Após a chamada, esse bloco pode ser re-alocado por outra <code>malloc</code>.\n\n<b>(A) ✗</b>: a variável <code>p</code> em si continua a existir (com o mesmo valor — o endereço antigo). É a memória <i>apontada</i> que é libertada.\n<b>(B) ✗ pegadinha importante:</b> <code>free</code> NÃO põe <code>p</code> a NULL. <code>p</code> continua a guardar o endereço antigo (agora inválido) — chama-se \"<i>dangling pointer</i>\". Boa prática: <code>free(p); p = NULL;</code>.\n<b>(D) ✗</b>: imprimir é <code>printf</code>.",
    "Erros comuns: <b>double free</b> (chamar <code>free(p)</code> duas vezes — UB), <b>use-after-free</b> (usar <code>*p</code> depois de <code>free</code> — UB), <b>memory leak</b> (perder o apontador antes de fazer <code>free</code>).<br><a href=\"sistemas_operacoes_estudo__2_.html#9-alocação-dinâmica-de-memória\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §9 Alocação Dinâmica de Memória ↗</a>",
  ],
  [
    "Linguagem C",
    "30",
    "O que é um <i>memory leak</i>?",
    [
      "Memória alocada que nunca é libertada com free",
      "Memória degradada por desgaste físico",
      "Falha física do módulo RAM",
      "Tipo específico de exploit que rouba dados de memória",
    ],
    0,
    "<b>(A) ✓ correcta:</b> alocas memória, perdes a referência (ex: o apontador é sobrescrito ou sai de scope), e nunca chamas <code>free</code>. A memória continua reservada até o programa terminar — o SO recupera-a só nessa altura.\n\nEm programas de longa duração (servidores, daemons), leaks acumulam até o sistema ficar sem RAM e crashar.\n\n<b>(D) ✗</b>: nada a ver com vírus.\n<b>(C), (B) ✗</b>: leak é um bug de <i>software</i>, não de hardware. A memória física está intacta.",
    "Ferramentas para detectar leaks: <b>Valgrind</b> (Linux/Mac), <b>AddressSanitizer</b> (<code>-fsanitize=address</code>). Indispensáveis em projectos C grandes.<br><a href=\"sistemas_operacoes_estudo__2_.html#9-alocação-dinâmica-de-memória\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §9 Alocação Dinâmica de Memória ↗</a>",
  ],
  [
    "Linguagem C",
    "31",
    "Como acedes a um campo <code>nome</code> de uma struct <code>pessoa</code> (variável directa, não apontador)?",
    [
      "pessoa->nome",
      "pessoa.nome",
      "pessoa[nome]",
      "*pessoa.nome",
    ],
    1,
    "<b>(B) pessoa.nome ✓ correcta:</b> operador <code>.</code> (ponto) para aceder a campos de uma struct quando tens a struct directamente.\n\n<b>(A) -&gt; ✗</b>: usa-se quando tens um <i>apontador</i> para a struct: <code>p-&gt;nome</code> é açúcar sintáctico para <code>(*p).nome</code>.\n<b>(C) [] ✗</b>: índice de array, não acesso a struct.\n<b>(D) ✗</b>: <code>*pessoa.nome</code> só faria sentido se <code>nome</code> fosse um apontador, e dereferenciar-se-ia o seu conteúdo. Para a struct directa não se usa <code>*</code>.",
    "Regra simples: <b>variável → ponto</b>. <b>Apontador → seta</b>. <code>p.x</code> vs <code>p-&gt;x</code>.<br><a href=\"sistemas_operacoes_estudo__2_.html#10-estruturas-struct-e-typedefs\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §10 Estruturas (struct) e Typedefs ↗</a>",
  ],
  [
    "Linguagem C",
    "32",
    "Para que serve a palavra-chave <code>typedef</code> em C?",
    [
      "Define uma macro",
      "Cria um sinónimo",
      "Define uma constante",
      "Importa um tipo de outro ficheiro",
    ],
    1,
    "<b>(B) ✓ correcta:</b> <code>typedef &lt;tipo existente&gt; &lt;novo nome&gt;;</code> cria um <i>alias</i>. Não cria um tipo novo — é só renomeação para legibilidade.\n\nEx: <code>typedef unsigned long size_t;</code><br><code>typedef struct ponto { int x, y; } Ponto;</code><br>Permite usar <code>Ponto p;</code> em vez de <code>struct ponto p;</code>.\n\n<b>(C) ✗</b>: constantes definem-se com <code>#define</code> ou <code>const</code>.\n<b>(A) ✗</b>: macros são do pré-processador (<code>#define</code>). <code>typedef</code> é processado pelo <i>compilador</i>, com type-checking apropriado.\n<b>(D) ✗</b>: importação faz-se com <code>#include</code> de headers.",
    "Muito útil para apontadores de função: <code>typedef int (*Callback)(int, int);</code> — agora <code>Callback f</code> é mais legível que <code>int (*f)(int, int)</code>.<br><a href=\"sistemas_operacoes_estudo__2_.html#10-estruturas-struct-e-typedefs\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §10 Estruturas (struct) e Typedefs ↗</a>",
  ],
  [
    "Linguagem C",
    "33",
    "Numa função <code>int main(int argc, char *argv[])</code>, o que representa <code>argc</code>?",
    [
      "O número total de argumentos da linha de comandos",
      "O conteúdo do primeiro argumento",
      "O nome do programa",
      "O código de retorno",
    ],
    0,
    "<b>(A) ✓ correcta:</b> <code>argc</code> = \"<i>argument count</i>\". Conta TODOS os tokens da linha de comandos, incluindo o nome do programa em <code>argv[0]</code>. Ex: <code>./prog a b c</code> dá <code>argc == 4</code>.\n\n<b>(B) ✗</b>: o conteúdo dos argumentos está em <code>argv[i]</code>, não em <code>argc</code>.\n<b>(C) ✗</b>: o nome do programa está em <code>argv[0]</code>.\n<b>(D) ✗</b>: o código de retorno é o que <code>main</code> devolve com <code>return</code>.",
    "Verificar argumentos: <code>if (argc != 3) { printf(\"Uso: %s a b\\n\", argv[0]); return 1; }</code>. <code>argc &gt;= 1</code> sempre (porque <code>argv[0]</code> existe).<br><a href=\"sistemas_operacoes_estudo__2_.html#11-argumentos-da-linha-de-comandos\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §11 Argumentos da Linha de Comandos ↗</a>",
  ],
  [
    "Linguagem C",
    "34",
    "Em <code>int main(int argc, char *argv[])</code>, qual o valor de <code>argv[0]</code>?",
    [
      "O primeiro argumento real do programa",
      "O número de argumentos",
      "<code>NULL</code>",
      "O nome do programa",
    ],
    3,
    "<b>(D) ✓ correcta:</b> por convenção UNIX, <code>argv[0]</code> contém o nome (ou caminho) do programa tal como foi invocado pela shell. Ex: <code>./prog</code> → <code>argv[0] == \"./prog\"</code>.\n\nProgramas usam isto para mensagens de erro: <code>fprintf(stderr, \"%s: erro\\n\", argv[0])</code>. Algumas ferramentas mudam de comportamento conforme o nome (<code>busybox</code>, <code>vi</code>/<code>view</code>).\n\n<b>(A) ✗</b>: o primeiro argumento <i>real</i> está em <code>argv[1]</code>.\n<b>(C) ✗</b>: <code>argv[0]</code> nunca é NULL (em sistemas POSIX). NULL é <code>argv[argc]</code> (sentinela).\n<b>(B) ✗</b>: o número está em <code>argc</code>.",
    "Estrutura: <code>argv[0]</code>=programa, <code>argv[1]..argv[argc-1]</code>=argumentos, <code>argv[argc]</code>=NULL (útil para passar a <code>execv</code>).<br><a href=\"sistemas_operacoes_estudo__2_.html#11-argumentos-da-linha-de-comandos\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §11 Argumentos da Linha de Comandos ↗</a>",
  ],
  [
    "Linguagem C",
    "35",
    "O que faz a directiva <code>#include &lt;stdio.h&gt;</code>?",
    [
      "Indica ao pré-processador para incluir",
      "Liga a biblioteca stdio.h ao executável",
      "Importa as classes do stdio",
      "É equivalente a <code>import</code> em Java",
    ],
    0,
    "<b>(A) ✓ correcta:</b> <code>#include</code> é processada pelo pré-processador (1ª fase) e funciona como <i>copy-paste textual</i> — substitui-se literalmente pelo conteúdo do ficheiro indicado. Por isso headers contêm declarações (não definições).\n\n• <code>&lt;name&gt;</code>: procura nos directórios de sistema (<code>/usr/include</code>, etc.).\n• <code>\"name\"</code>: procura primeiro no directório actual, depois nos do sistema.\n\n<b>(B) ✗ pegadinha importante:</b> <code>#include</code> NÃO linka bibliotecas — só insere declarações. A linkagem da biblioteca correspondente faz-se com a flag <code>-l</code> ao linker (ex: <code>-lm</code> para math).\n<b>(C) ✗</b>: C não tem classes (isso é C++/Java).\n<b>(D) ✗</b>: <code>import</code> de Java é semanticamente muito diferente — só importa nomes, não inclui código.",
    "Para usar <code>printf</code> precisas: (1) <code>#include &lt;stdio.h&gt;</code> (declaração) E (2) link com a libc (que é automático com <code>gcc</code>).<br><a href=\"sistemas_operacoes_estudo__2_.html#12-o-pré-processador-c\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §12 O Pré-processador C ↗</a>",
  ],
  [
    "Linguagem C",
    "36",
    "O que faz a directiva <code>#define PI 3.14</code>?",
    [
      "Declara uma variável <code>PI</code>",
      "Importa <code>PI</code> de uma biblioteca",
      "Define uma macro",
      "Aloca memória para <code>PI</code>",
    ],
    2,
    "<b>(C) ✓ correcta:</b> <code>#define</code> faz <i>substituição textual</i> antes do compilador ver o código. Não é variável — não tem tipo, não ocupa memória, não pode ser endereçada com <code>&amp;</code>. Por convenção, macros em MAIÚSCULAS para não confundir com variáveis.\n\n<b>(A) ✗</b>: variáveis declaram-se com tipos (<code>const double pi = 3.14;</code>). Em C moderno, <code>const</code> é geralmente preferível a <code>#define</code> para constantes (tem type-checking).\n<b>(D) ✗</b>: macros não ocupam memória — são puramente textuais.\n<b>(B) ✗</b>: importação é <code>#include</code>.",
    "Macros podem ter argumentos: <code>#define MAX(a,b) ((a)&gt;(b)?(a):(b))</code>. Cuidado com efeitos colaterais: <code>MAX(i++, j++)</code> incrementa duas vezes! Por isso, em C moderno, prefere <code>inline</code> functions.<br><a href=\"sistemas_operacoes_estudo__2_.html#12-o-pré-processador-c\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §12 O Pré-processador C ↗</a>",
  ],
  [
    "Linguagem C",
    "37",
    "Para que serve a \"include guard\" típica <code>#ifndef HEADER_H / #define HEADER_H / ... / #endif</code>?",
    [
      "Para acelerar a compilação",
      "Para condicionar plataformas",
      "Para impedir inclusão múltipla do mesmo header",
      "Para fazer debug",
    ],
    2,
    "<b>(C) ✓ correcta:</b> sem guard, se o mesmo header for incluído duas vezes (directa ou indirectamente via outros headers), todos os <code>typedef</code>, <code>struct</code>, etc. seriam declarados duas vezes → erro de compilação.\n\nComo funciona: na 1ª inclusão, <code>HEADER_H</code> não está definido → entra no bloco e define-o. Nas inclusões seguintes, já está definido → bloco é saltado.\n\n<b>(A) ✗</b>: pode até <i>acelerar</i> ligeiramente (evita re-processar o conteúdo), mas esse não é o propósito.\n<b>(D) ✗</b>: nada a ver com debug.\n<b>(B) ✗</b>: para condicionar plataformas usam-se outras directivas como <code>#ifdef _WIN32</code>, <code>#ifdef __linux__</code>.",
    "Alternativa moderna (não-standard mas suportada por todos os compiladores): <code>#pragma once</code> no topo do header, sem precisar de #ifndef/#define/#endif.<br><a href=\"sistemas_operacoes_estudo__2_.html#12-o-pré-processador-c\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §12 O Pré-processador C ↗</a>",
  ],
  [
    "Linguagem C",
    "38",
    "Em Unix, em que ficheiros está tipicamente uma biblioteca dinâmica e o seu header?",
    [
      "<code>.h</code> e <code>.c</code> respectivamente",
      "<code>.dll</code> e <code>.lib</code> respectivamente",
      "<code>.so</code> e <code>.h</code> respectivamente",
      "<code>.bin</code> e <code>.src</code> respectivamente",
    ],
    2,
    "<b>(C) ✓ correcta:</b>\n• <b><code>.h</code></b>: <i>header</i>. Contém declarações (protótipos de funções, tipos, macros). É lido pelo compilador na fase de pré-processamento.\n• <b><code>.so</code></b>: \"<i>shared object</i>\". Biblioteca dinâmica binária pré-compilada, carregada em runtime.\n\n<b>(B) ✗</b>: <code>.dll</code> e <code>.lib</code> são equivalentes em <i>Windows</i> (<code>.dll</code> = dinâmica, <code>.lib</code> pode ser estática ou stub para .dll), não em Unix.\n<b>(A) ✗</b>: <code>.c</code> é código-fonte, não biblioteca.\n<b>(D) ✗</b>: extensões fictícias.",
    "Resumo Unix: <code>.h</code> (header), <code>.c</code> (fonte), <code>.o</code> (objecto), <code>.a</code> (estática, \"archive\"), <code>.so</code> (dinâmica, \"shared object\").<br><a href=\"sistemas_operacoes_estudo__2_.html#13-bibliotecas-em-c\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §13 Bibliotecas em C ↗</a>",
  ],
  [
    "Linguagem C",
    "39",
    "Para usar a função <code>sqrt()</code> (raiz quadrada) da biblioteca matemática em C, qual a flag necessária para o gcc?",
    [
      "-lmath",
      "Não é necessária flag, está em libc",
      "-math",
      "-lm",
    ],
    3,
    "<b>(D) -lm ✓ correcta:</b> <code>sqrt</code> e outras funções matemáticas estão em <code>libm</code> (biblioteca matemática), separada da libc padrão. Tem de se: (1) <code>#include &lt;math.h&gt;</code> para a declaração, E (2) compilar com <code>-lm</code> para o linker encontrar a implementação.\n\n<b>(A) -lmath ✗</b>: a biblioteca chama-se <code>libm</code>, não <code>libmath</code>. A flag <code>-l&lt;name&gt;</code> procura por <code>lib&lt;name&gt;.so</code> ou <code>lib&lt;name&gt;.a</code>.\n<b>(C) -math ✗</b>: não existe.\n<b>(B) ✗</b>: não está em libc — é uma biblioteca separada por razões históricas.",
    "Outras flags <code>-l</code> comuns: <code>-lpthread</code> (threads), <code>-lcrypto</code> (OpenSSL), <code>-lz</code> (zlib). Convenção: a flag tem de vir DEPOIS dos ficheiros que a usam.<br><a href=\"sistemas_operacoes_estudo__2_.html#13-bibliotecas-em-c\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §13 Bibliotecas em C ↗</a>",
  ],
  [
    "Linguagem C",
    "40",
    "Qual é o output do <i>compilador</i> e qual é o output do <i>linker</i>?",
    [
      "Ambos produzem assembly",
      "Compilador produz <code>.o</code>",
      "Ambos produzem executável",
      "Compilador produz executável; linker produz <code>.o</code>",
    ],
    1,
    "<b>(B) ✓ correcta:</b>\n• <b>Compilador</b> (<code>cc1</code>): <code>.c</code> → assembly → <code>.o</code> (código-objecto, com referências externas não resolvidas).\n• <b>Linker</b> (<code>ld</code>): junta vários <code>.o</code> + bibliotecas, resolve as referências entre eles, produz <i>executável</i>.\n\nÉ por isso que <code>printf</code> compila sem erro mesmo sem <code>libc</code> — é só na fase de link que dá \"undefined reference\".\n\n<b>(C) ✗</b>: só o linker.\n<b>(D) ✗</b>: invertido.\n<b>(A) ✗</b>: assembly é etapa <i>intermédia</i> dentro do compilador.",
    "Pipeline: <code>.c</code> → preproc → <code>.i</code> → compiler → <code>.s</code> → assembler → <code>.o</code> → linker → executável.<br><a href=\"sistemas_operacoes_estudo__2_.html#14-compilacao-e-linkagem\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §14 Compilacao e Linkagem ↗</a>",
  ],
  [
    "Linguagem C",
    "41",
    "Qual a função do \"linker\" no processo de construção de um programa C?",
    [
      "Optimiza o código",
      "Verifica a sintaxe do código",
      "Carrega o programa em memória",
      "Junta os ficheiros objecto",
    ],
    3,
    "<b>(D) ✓ correcta:</b> o linker pega em todos os <code>.o</code> gerados pelo compilador + bibliotecas necessárias e:\n1. Resolve referências cruzadas (ex: o teu <code>main.o</code> chama <code>printf</code> → linker procura em <code>libc.so</code> e regista o endereço).\n2. Junta tudo num único executável.\n3. Atribui endereços finais a cada símbolo.\n\n<b>(B) ✗</b>: sintaxe é verificada pelo <i>compilador</i>. O linker pode dar erros mas só de \"símbolo não encontrado\" ou \"símbolo duplicado\".\n<b>(C) ✗</b>: carregar em memória é o trabalho do <i>loader</i> (parte do SO) em runtime.\n<b>(A) ✗</b>: optimização é do compilador (<code>-O0</code>, <code>-O1</code>, <code>-O2</code>, <code>-O3</code>).",
    "Sequência completa: Pré-processador → Compilador → <b>Linker</b> → (executável) → Loader (em runtime).<br><a href=\"sistemas_operacoes_estudo__2_.html#14-compilação-e-linkagem\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §14 Compilação e Linkagem ↗</a>",
  ],
  [
    "Linguagem C",
    "42",
    "Qual a diferença entre linkagem ESTÁTICA e DINÂMICA?",
    [
      "Funcionalmente idênticas em runtime",
      "Estática só funciona em sistemas Unix",
      "Dinâmica é sempre mais rápida",
      "Estática copia código; dinâmica referencia em runtime",
    ],
    3,
    "<b>(D) ✓ correcta:</b>\n• <b>Estática</b> (<code>.a</code>/<code>.lib</code>): o código da biblioteca é incorporado no executável. Vantagem: o binário é autocontido, funciona em qualquer máquina mesmo sem a lib. Desvantagem: tamanho enorme, e cada programa duplica o código (ex: 100 programas com a mesma libc → 100 cópias na RAM).\n• <b>Dinâmica</b> (<code>.so</code>/<code>.dll</code>): o executável guarda só uma referência ao nome da biblioteca. O loader procura-a em runtime e carrega-a uma única vez na RAM, partilhada por todos os programas. Vantagem: poupa espaço e permite actualizar a biblioteca sem recompilar (ex: patch de segurança). Desvantagem: depende da lib estar presente (\"DLL hell\").\n\n<b>(A) ✗</b>: têm trade-offs muito diferentes.\n<b>(C) ✗</b>: depende do contexto. Dinâmica tem ligeiro overhead na primeira chamada (resolução de símbolo), mas em estado estável é igualmente rápida.\n<b>(B) ✗</b>: ambas existem em todos os SOs (Windows tem <code>.lib</code> estática e <code>.dll</code> dinâmica).",
    "Em embedded e em distribuições \"static binary\" (Go, Rust com musl) prefere-se estática para portabilidade. Em sistemas Linux normais, quase tudo é dinâmico.<br><a href=\"sistemas_operacoes_estudo__2_.html#15-linkagem-estática-vs-dinâmica\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §15 Linkagem Estática vs Dinâmica ↗</a>",
  ],
  [
    "Linguagem C",
    "43",
    "Quando se faz <i>linkagem estática</i> de um programa que usa apenas <code>sin()</code> e <code>cos()</code> da <code>libm</code>, o que é incluído no executável?",
    [
      "Nada — libm é sempre dinâmica",
      "A biblioteca libm inteira é incluída",
      "Apenas sin.o e cos.o que são usados",
      "Apenas o header math.h é colado",
    ],
    2,
    "<b>(C) ✓ correcta:</b> o linker é \"inteligente\" — só inclui os <code>.o</code> que o programa <i>realmente referencia</i>. A <code>libm.a</code> é um <i>archive</i> com um <code>.o</code> por função; o linker percorre as referências não resolvidas e puxa só os <code>.o</code> necessários.\n\nResultado: o executável final fica <i>muito mais pequeno</i> do que se a biblioteca inteira fosse anexada.\n\n<b>(B) ✗</b>: seria desperdício (<code>libm</code> tem dezenas de funções).\n<b>(D) ✗</b>: o header só tem assinaturas — não tem código compilado.\n<b>(A) ✗</b>: <code>libm</code> existe nas duas formas (<code>libm.a</code> estática e <code>libm.so</code> dinâmica).",
    "Para listar o conteúdo: <code>ar -t libm.a</code> (estática) ou <code>nm -D --defined-only libm.so</code> (dinâmica).<br><a href=\"sistemas_operacoes_estudo__2_.html#16-como-e-uma-biblioteca-por-dentro\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §16 Como é uma Biblioteca por dentro ↗</a>",
  ],
  [
    "Linguagem C",
    "44",
    "O que tipicamente contém o header <code>math.h</code>?",
    [
      "Os ficheiros <code>.o</code> da biblioteca",
      "O código compilado das funções matemáticas",
      "Os <code>#define</code> de constantes",
      "O loader em runtime",
    ],
    2,
    "<b>(C) ✓ correcta:</b> headers têm <i>declarações</i>, não <i>definições</i>. <code>math.h</code> contém:\n• <code>#define</code> de constantes matemáticas (<code>M_PI</code>, <code>M_E</code>, …).\n• Assinaturas das funções: <code>double cos(double x);</code>, <code>double sin(double x);</code>, etc.\nUsado pelo compilador para fazer <i>type-checking</i> (verificar se chamas as funções com os tipos certos).\n\n<b>(B) ✗</b>: o código compilado está na biblioteca (<code>libm.a</code>/<code>libm.so</code>), não no header.\n<b>(A) ✗</b>: <code>.o</code> também ficam na biblioteca.\n<b>(D) ✗</b>: o loader é parte do SO/runtime, não do header.",
    "Headers tipicamente em <code>/usr/include/</code>. Bibliotecas tipicamente em <code>/usr/lib/</code>.<br><a href=\"sistemas_operacoes_estudo__2_.html#16-como-e-uma-biblioteca-por-dentro\" target=\"_blank\" style=\"color:#5b8af0;text-decoration:none;border:1px solid rgba(91,138,240,.35);padding:2px 8px;border-radius:6px;display:inline-block;margin-top:6px;font-size:.78rem;\">📖 §16 Como é uma Biblioteca por dentro ↗</a>",
  ],
];
